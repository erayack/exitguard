apiVersion: v1
kind: Service
metadata:
  name: exitguard-scanner-metrics
  namespace: exitguard-system
  labels:
    app.kubernetes.io/name: exitguard
    app.kubernetes.io/component: scanner
spec:
  selector:
    app.kubernetes.io/name: exitguard
    app.kubernetes.io/component: scanner
  ports:
    - name: https
      port: 8443
      targetPort: https-metrics
      protocol: TCP
