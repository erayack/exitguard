apiVersion: v1
kind: Service
metadata:
  name: exitguard-executor-metrics
  namespace: exitguard-system
  labels:
    app.kubernetes.io/name: exitguard
    app.kubernetes.io/component: executor
spec:
  selector:
    app.kubernetes.io/name: exitguard
    app.kubernetes.io/component: executor
  ports:
    - name: https
      port: 8443
      targetPort: https-metrics
      protocol: TCP
