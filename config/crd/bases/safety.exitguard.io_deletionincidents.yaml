---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: deletionincidents.safety.exitguard.io
spec:
  group: safety.exitguard.io
  names:
    kind: DeletionIncident
    listKind: DeletionIncidentList
    plural: deletionincidents
    shortNames:
    - dincident
    - di
    singular: deletionincident
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.target.kind
      name: Kind
      type: string
    - jsonPath: .spec.target.namespace
      name: Namespace
      type: string
    - jsonPath: .spec.target.name
      name: Target
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeletionIncident is a durable diagnosis of one deleting object
          UID.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeletionIncidentSpec pins an incident to one target incarnation.
            properties:
              firstObservedTime:
                description: FirstObservedTime is when the scanner first observed
                  the target past its threshold.
                format: date-time
                type: string
              target:
                description: Target is the deleting Kubernetes object.
                properties:
                  apiGroup:
                    description: APIGroup is empty for the Kubernetes core API group.
                    type: string
                  kind:
                    description: Kind is the Kubernetes Kind reported by discovery.
                    minLength: 1
                    type: string
                  name:
                    description: Name is the object name.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace is empty for cluster-scoped resources.
                    type: string
                  resource:
                    description: Resource is the plural REST resource name.
                    minLength: 1
                    type: string
                  uid:
                    description: UID prevents a same-name replacement from receiving
                      an old mutation.
                    type: string
                  version:
                    description: Version is the served API version used for this observation.
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                - resource
                - uid
                - version
                type: object
                x-kubernetes-validations:
                - message: uid must not be empty
                  rule: self.uid.size() > 0
            required:
            - firstObservedTime
            - target
            type: object
            x-kubernetes-validations:
            - message: spec is immutable
              rule: self == oldSelf
          status:
            description: DeletionIncidentStatus contains scanner-owned diagnosis state.
            properties:
              actionEvidenceExpiresTime:
                description: ActionEvidenceExpiresTime is the fail-closed deadline
                  for executing RecommendedActions.
                format: date-time
                type: string
              actionEvidenceTime:
                description: ActionEvidenceTime is when the evidence backing RecommendedActions
                  was last refreshed.
                format: date-time
                type: string
              activePolicyRef:
                description: ActivePolicyRef is the current winning policy and generation.
                properties:
                  generation:
                    description: Generation is the policy generation used to classify
                      actions.
                    format: int64
                    type: integer
                  name:
                    description: Name is the policy name.
                    type: string
                  uid:
                    description: UID pins the policy incarnation.
                    type: string
                required:
                - generation
                - name
                - uid
                type: object
              conditions:
                description: Conditions report target visibility and diagnosis completeness.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deletionTimestamp:
                description: DeletionTimestamp is copied from the current target.
                format: date-time
                type: string
              findings:
                description: Findings are deterministic, current diagnostic observations.
                items:
                  description: Finding is one deterministic diagnostic observation.
                  properties:
                    apiService:
                      description: APIService is an unavailable aggregated API service
                        name.
                      type: string
                    controllerRef:
                      description: ControllerRef is the configured owner whose health
                        was evaluated.
                      properties:
                        apiVersion:
                          description: APIVersion must identify the apps API used
                            by the controller workload.
                          enum:
                          - apps/v1
                          type: string
                        kind:
                          description: Kind is a supported controller workload.
                          enum:
                          - Deployment
                          - StatefulSet
                          - DaemonSet
                          type: string
                        name:
                          description: Name is the controller workload name.
                          minLength: 1
                          type: string
                        namespace:
                          description: Namespace contains the controller workload.
                          minLength: 1
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      - namespace
                      type: object
                    count:
                      description: Count summarizes repeated evidence when individual
                        entries are omitted.
                      format: int32
                      minimum: 0
                      type: integer
                    finalizer:
                      description: Finalizer is the exact blocking finalizer.
                      type: string
                    id:
                      description: ID is stable while the same evidence remains present.
                      minLength: 1
                      type: string
                    message:
                      description: Message is a bounded human-readable explanation.
                      maxLength: 1024
                      type: string
                    resourceRef:
                      description: ResourceRef identifies a related blocking object.
                      properties:
                        apiGroup:
                          description: APIGroup is empty for the Kubernetes core API
                            group.
                          type: string
                        kind:
                          description: Kind is the Kubernetes Kind reported by discovery.
                          minLength: 1
                          type: string
                        name:
                          description: Name is the object name.
                          minLength: 1
                          type: string
                        namespace:
                          description: Namespace is empty for cluster-scoped resources.
                          type: string
                        resource:
                          description: Resource is the plural REST resource name.
                          minLength: 1
                          type: string
                        uid:
                          description: UID prevents a same-name replacement from receiving
                            an old mutation.
                          type: string
                        version:
                          description: Version is the served API version used for
                            this observation.
                          minLength: 1
                          type: string
                      required:
                      - kind
                      - name
                      - resource
                      - uid
                      - version
                      type: object
                      x-kubernetes-validations:
                      - message: uid must not be empty
                        rule: self.uid.size() > 0
                    truncated:
                      description: Truncated states that diagnosis stopped at a configured
                        safety limit.
                      type: boolean
                    type:
                      description: Type classifies the observation.
                      enum:
                      - BlockingFinalizer
                      - RemainingResource
                      - UnavailableAPIService
                      - DiscoveryFailure
                      - BlockingWebhook
                      - MissingFinalizerController
                      - DeletionGracePeriodExceeded
                      - ResourceTypeRemoved
                      - PolicyDisallowsRemediation
                      - Unknown
                      type: string
                    webhook:
                      description: Webhook is a matching admission webhook configuration/name
                        pair.
                      type: string
                  required:
                  - id
                  - message
                  - type
                  type: object
                maxItems: 513
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              lastObservedTime:
                description: LastObservedTime is periodically refreshed while the
                  target remains visible.
                format: date-time
                type: string
              phase:
                description: Phase is the current incident lifecycle state.
                enum:
                - Active
                - DiagnosisFailed
                - Resolved
                type: string
              recommendedActions:
                description: RecommendedActions are policy-classified operations available
                  for approval.
                items:
                  description: RemediationAction is one policy-classified operation
                    available for approval.
                  properties:
                    eligible:
                      description: Eligible records whether the active policy permits
                        approval.
                      type: boolean
                    finalizer:
                      description: Finalizer is required by finalizer-removal actions.
                      type: string
                    id:
                      description: ID is a deterministic digest of action type, target
                        UID, finalizer, target resource version, policy UID, and policy
                        generation.
                      minLength: 1
                      type: string
                    ineligibleReason:
                      description: IneligibleReason explains a policy or evidence
                        restriction.
                      maxLength: 512
                      type: string
                    preconditionResourceVersion:
                      description: PreconditionResourceVersion is the version observed
                        during diagnosis.
                      type: string
                    reason:
                      description: Reason explains why this action addresses the finding.
                      maxLength: 1024
                      type: string
                    risk:
                      description: Risk is fixed by the operator and cannot be lowered
                        by policy.
                      enum:
                      - None
                      - Low
                      - Medium
                      - High
                      - Critical
                      type: string
                    target:
                      description: Target pins the operation to one object UID.
                      properties:
                        apiGroup:
                          description: APIGroup is empty for the Kubernetes core API
                            group.
                          type: string
                        kind:
                          description: Kind is the Kubernetes Kind reported by discovery.
                          minLength: 1
                          type: string
                        name:
                          description: Name is the object name.
                          minLength: 1
                          type: string
                        namespace:
                          description: Namespace is empty for cluster-scoped resources.
                          type: string
                        resource:
                          description: Resource is the plural REST resource name.
                          minLength: 1
                          type: string
                        uid:
                          description: UID prevents a same-name replacement from receiving
                            an old mutation.
                          type: string
                        version:
                          description: Version is the served API version used for
                            this observation.
                          minLength: 1
                          type: string
                      required:
                      - kind
                      - name
                      - resource
                      - uid
                      - version
                      type: object
                      x-kubernetes-validations:
                      - message: uid must not be empty
                        rule: self.uid.size() > 0
                    type:
                      description: Type selects the executor operation.
                      enum:
                      - RemoveResourceFinalizer
                      - RemoveCRDFinalizer
                      - ForceFinalizeNamespace
                      type: string
                  required:
                  - eligible
                  - id
                  - reason
                  - risk
                  - target
                  - type
                  type: object
                maxItems: 256
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              resolutionReason:
                description: ResolutionReason explains why the incident became resolved.
                maxLength: 512
                type: string
              resolvedTime:
                description: ResolvedTime is set when the target disappears or leaves
                  monitoring scope.
                format: date-time
                type: string
              targetSnapshot:
                description: TargetSnapshot contains the exact mutation preconditions
                  observed by diagnosis.
                properties:
                  metadataFinalizers:
                    description: MetadataFinalizers is the complete metadata finalizer
                      set.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  namespaceFinalizers:
                    description: NamespaceFinalizers is populated from Namespace spec.finalizers.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  resourceVersion:
                    description: ResourceVersion is the API concurrency token observed
                      by the scanner.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
