---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: remediationapprovals.safety.exitguard.io
spec:
  group: safety.exitguard.io
  names:
    kind: RemediationApproval
    listKind: RemediationApprovalList
    plural: remediationapprovals
    shortNames:
    - rapproval
    - ra
    singular: remediationapproval
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.result
      name: Result
      type: string
    - jsonPath: .spec.incidentRef.name
      name: Incident
      type: string
    - jsonPath: .spec.dryRun
      name: DryRun
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RemediationApproval authorizes one policy-eligible action. Creating
          this object is the approval act.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RemediationApprovalSpec approves one current incident action.
            properties:
              actionID:
                description: ActionID identifies one action in the incident's current
                  status.
                minLength: 1
                type: string
              dryRun:
                description: DryRun asks the API server to validate without persisting
                  the mutation.
                type: boolean
              incidentRef:
                description: IncidentRef pins the approval to one DeletionIncident
                  incarnation.
                properties:
                  name:
                    description: Name is the referenced object's name.
                    minLength: 1
                    type: string
                  uid:
                    description: UID pins the reference to one object incarnation.
                    type: string
                required:
                - name
                - uid
                type: object
                x-kubernetes-validations:
                - message: uid must not be empty
                  rule: self.uid.size() > 0
              reason:
                description: Reason is the operator-provided justification for this
                  approval.
                maxLength: 512
                minLength: 1
                type: string
            required:
            - actionID
            - incidentRef
            - reason
            type: object
            x-kubernetes-validations:
            - message: spec is immutable
              rule: self == oldSelf
          status:
            description: RemediationApprovalStatus is the executor-owned lifecycle
              and audit record.
            properties:
              action:
                description: Action preserves the exact validated action before any
                  mutation begins.
                properties:
                  eligible:
                    description: Eligible records whether the active policy permits
                      approval.
                    type: boolean
                  finalizer:
                    description: Finalizer is required by finalizer-removal actions.
                    type: string
                  id:
                    description: ID is a deterministic digest of action type, target
                      UID, finalizer, target resource version, policy UID, and policy
                      generation.
                    minLength: 1
                    type: string
                  ineligibleReason:
                    description: IneligibleReason explains a policy or evidence restriction.
                    maxLength: 512
                    type: string
                  preconditionResourceVersion:
                    description: PreconditionResourceVersion is the version observed
                      during diagnosis.
                    type: string
                  reason:
                    description: Reason explains why this action addresses the finding.
                    maxLength: 1024
                    type: string
                  risk:
                    description: Risk is fixed by the operator and cannot be lowered
                      by policy.
                    enum:
                    - None
                    - Low
                    - Medium
                    - High
                    - Critical
                    type: string
                  target:
                    description: Target pins the operation to one object UID.
                    properties:
                      apiGroup:
                        description: APIGroup is empty for the Kubernetes core API
                          group.
                        type: string
                      kind:
                        description: Kind is the Kubernetes Kind reported by discovery.
                        minLength: 1
                        type: string
                      name:
                        description: Name is the object name.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace is empty for cluster-scoped resources.
                        type: string
                      resource:
                        description: Resource is the plural REST resource name.
                        minLength: 1
                        type: string
                      uid:
                        description: UID prevents a same-name replacement from receiving
                          an old mutation.
                        type: string
                      version:
                        description: Version is the served API version used for this
                          observation.
                        minLength: 1
                        type: string
                    required:
                    - kind
                    - name
                    - resource
                    - uid
                    - version
                    type: object
                    x-kubernetes-validations:
                    - message: uid must not be empty
                      rule: self.uid.size() > 0
                  type:
                    description: Type selects the executor operation.
                    enum:
                    - RemoveResourceFinalizer
                    - RemoveCRDFinalizer
                    - ForceFinalizeNamespace
                    type: string
                required:
                - eligible
                - id
                - reason
                - risk
                - target
                - type
                type: object
              attempts:
                description: Attempts retains at most the latest twenty execution
                  attempts.
                items:
                  description: RemediationAttempt is one bounded, sanitized executor
                    audit entry.
                  properties:
                    actionType:
                      description: ActionType is the operation attempted.
                      enum:
                      - RemoveResourceFinalizer
                      - RemoveCRDFinalizer
                      - ForceFinalizeNamespace
                      type: string
                    dryRun:
                      description: DryRun records whether persistence was disabled.
                      type: boolean
                    errorReason:
                      description: ErrorReason is a stable, non-sensitive API or validation
                        reason.
                      maxLength: 128
                      type: string
                    message:
                      description: Message is a sanitized bounded explanation without
                        resource contents.
                      maxLength: 1024
                      type: string
                    resourceVersionAfter:
                      description: ResourceVersionAfter is populated when the API
                        returns a persisted version.
                      type: string
                    resourceVersionBefore:
                      description: ResourceVersionBefore is the concurrency token
                        validated before execution.
                      type: string
                    result:
                      description: Result is this attempt's outcome.
                      enum:
                      - Mutated
                      - AlreadySatisfied
                      - DryRunValidated
                      - RejectedByPolicy
                      - TargetReplaced
                      - TargetChanged
                      - APIError
                      type: string
                    time:
                      description: Time is when this attempt completed.
                      format: date-time
                      type: string
                  required:
                  - actionType
                  - dryRun
                  - result
                  - time
                  type: object
                maxItems: 20
                type: array
                x-kubernetes-list-type: atomic
              completedTime:
                description: CompletedTime is set for every terminal phase.
                format: date-time
                type: string
              conditions:
                description: Conditions report validation, execution, and terminal
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the immutable spec generation processed
                  by the executor.
                format: int64
                type: integer
              phase:
                description: Phase is the current approval lifecycle state.
                enum:
                - Pending
                - Running
                - Succeeded
                - DryRunSucceeded
                - Failed
                - Expired
                - Superseded
                type: string
              result:
                description: Result is the latest or terminal execution result.
                enum:
                - Mutated
                - AlreadySatisfied
                - DryRunValidated
                - RejectedByPolicy
                - TargetReplaced
                - TargetChanged
                - APIError
                type: string
              startedTime:
                description: StartedTime is when execution first began.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
